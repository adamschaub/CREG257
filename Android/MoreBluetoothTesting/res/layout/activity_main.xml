<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/initButtonId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Initialize" />

    <Button
        android:id="@+id/lockButtonId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:text="Lock" />

    <Button
        android:id="@+id/unlockButtonId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/lockButtonId"
        android:layout_alignBottom="@+id/lockButtonId"
        android:layout_alignRight="@+id/initButtonId"
        android:text="Unlock" />

    <Button
        android:id="@+id/shareKeyButtonId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/initButtonId"
        android:layout_alignBottom="@+id/initButtonId"
        android:layout_alignLeft="@+id/lockButtonId"
        android:text="Share Key" />

    <Button
        android:id="@+id/delButtonId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/lockButtonId"
        android:layout_below="@+id/lockButtonId"
        android:layout_marginTop="68dp"
        android:text="Delete Lock Files" />

</RelativeLayout>